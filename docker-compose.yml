version: '3.8'

services:
<<<<<<< HEAD
    php:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: php-fpm
        volumes:
            - .:/var/www/html
        networks:
            - app-network
        depends_on:
            - postgres

    nginx:
        image: nginx:latest
        container_name: nginx
        volumes:
            - ./nginx/default.conf:/etc/nginx/conf.d/default.conf  # Убедитесь, что это правильный путь
            - .:/var/www/html
        ports:
            - "800:80"
        networks:
            - app-network
        depends_on:
            - php

    postgres:
        image: postgres:13
        container_name: postgres
        environment:
            POSTGRES_DB: laravel_docker
            POSTGRES_USER: user
            POSTGRES_PASSWORD: user
        volumes:
            - postgres_data:/var/lib/postgresql/data
        networks:
            - app-network

networks:
    app-network:
        driver: bridge

volumes:
    postgres_data:
=======
    app:
        build:
            context: .
            dockerfile: docker/Dockerfile
        container_name: tg-bot-mlbb
        restart: always
        working_dir: /var/www
        volumes:
            - .:/var/www
        depends_on:
            - db
            - redis
        networks:
            - bot-network

    db:
        image: postgres:15
        container_name: postgres-db
        restart: always
        environment:
            POSTGRES_DB: bot_db
            POSTGRES_USER: bot_user
            POSTGRES_PASSWORD: bot_password
        ports:
            - "5432:5432"
        networks:
            - bot-network

    redis:
        image: redis:latest
        container_name: redis-cache
        restart: always
        networks:
            - bot-network

networks:
    bot-network:
        driver: bridge
>>>>>>> 9d9ee22 (project changes + VM host from Yandex Cloud)
